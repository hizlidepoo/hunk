---
name: Bug report
about: Something hunk got wrong
labels: bug
---

## What happened

<!-- What you saw. A screenshot or a paste of the screen is worth a lot here. -->

## What you expected

## How to reproduce

<!--
The diff matters. If you can, attach the exact input:

    git diff > bug.diff      # then attach bug.diff
    hunk < bug.diff

If it only happens on your own code, a reduced version that still misbehaves is
much more useful than a description.
-->

```
hunk ...
```

## Environment

- hunk version or commit:
- OS and terminal (e.g. macOS 15, iTerm2 / Ubuntu 24.04, kitty):
- `$TERM` and `$COLORTERM`:
- Theme (`--theme` / `$HUNK_THEME`), if not the default:
- `git --version`, if it involves the working-tree review mode:

## Anything else

<!-- If staging did something unexpected, please include `git status` output. -->
